# riscv-single-cycle-processor
This project implements a basic RV32I Single Cycle Processor in Verilog.  The processor executes a subset of RISC-V instructions including ADD and ADDI and demonstrates the complete datapath from instruction fetch to register write-back.
